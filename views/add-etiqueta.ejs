<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/styles/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>

</html>
<body>
    <%- include('index.ejs') %>
    <div class="container mt-3">
        <h1>Crear nuevas etiquetas</h1>
        <form action="new-etiqueta" method="POST">
            <div class="mb-3 mt-3">
                <label for="tag_name">Etiqueta</label>
                <input class="form-control" type="text" id="tag_name" name="tag_name">
            </div>
            <button type="submit">Crear</button>
        </form>
    </div>
    
    <br>
    
    <br>
    
    
    <div class="container mt-3">
        <h2>Agregar etiquetas</h2>
        <form action="/etiqueta-to-prod" method="POST">
            <div class="mb-3 mt-3">
                <label for="prod">Selecciona un producto</label>
                <select class="form-control form-control-sm" name="prod" id="prod">
                    <% prod.forEach(function(prod) { %>
                        <option value=<%=prod.id_pro %>><%= prod.nombre_pro %>
                        </option>
                        <% }); %>
                </select>
            </div>
            <div class="mb-3 mt-3">
                <label for="etiqueta">Agrega una etiqueta</label>
                <select class="form-control form-control-sm" name="etiqueta" id="etiqueta">
                    <% etiqueta.forEach(function(etiqueta) { %>
                        <option value=<%=etiqueta.id_etiqueta %>><%= etiqueta.nombre_etiqueta %>
                        </option>
                        <% }); %>
                </select>
            </div>
            <button type="submit">Guardar</button>
        </form>
    </div>
    
    <a href="/">Index</a>
</body>
</html>